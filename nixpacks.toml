# Railway/Nixpacks configuration
[phases.setup]
nixPkgs = ["python311", "pip"]

[phases.install]
cmds = [
    "pip install uv",
    "cd backend && uv sync --frozen"
]

[start]
cmd = "cd backend && uv run uvicorn app:app --host 0.0.0.0 --port $PORT"
